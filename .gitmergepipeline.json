{
  "rules": {
    "allFiles": {
      "type": "filePattern",
      "pattern": ".*"
    }
  },
  "pipelines": [
    {
      "type": "standard",
      "name": "Default pipeline for all files",
      "description": "Default pipeline for all files",
      "condition": "allFiles",
      "steps": [
        {
          "operation": "take-other"
        }
      ]
    }
  ]
}
